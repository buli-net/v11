# =================================================================
# FILE: .github/workflows/build-mainnet.yml
# MỤC ĐÍCH: Build MainnetDebug APK (tương thích bitcoinj 0.17.1)
# DỰA TRÊN: build.Containerfile (cách cài SDK, gradle) và workflow cũ
# =================================================================

name: Build Bitcoin Wallet - MainnetDebug (v11.04 + bitcoinj 0.17)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      # ----- 1. CLONE TỪ GITLAB VỚI ĐÚNG TAG (hoặc dùng actions/checkout) -----
      - name: Clone repository (tag v11.04)
        run: |
          git clone --depth 1 --branch v11.04 https://gitlab.com/bitcoin-wallet/bitcoin-wallet.git .
          # Nếu repo của bạn ở GitHub, thay bằng lệnh clone hoặc dùng actions/checkout

      # ----- 2. TẠO CÁC THƯ MỤC RES-MAINNET & ASSETS-MAINNET (nếu chưa có) -----
      #     Điều này rất quan trọng để tránh lỗi resource not found khi chạy app
      - name: Prepare Mainnet source sets
        run: |
          mkdir -p wallet/res-mainnet wallet/assets-mainnet
          # Nếu cần, có thể copy các file mẫu từ thư mục main
          # cp -r wallet/res/* wallet/res-mainnet/ 2>/dev/null || true
          # cp -r wallet/assets/* wallet/assets-mainnet/ 2>/dev/null || true

      # ----- 3. CÀI JDK 17 (DÙNG CHO SDK MANAGER) -----
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # ----- 4. CÀI ĐẶT ANDROID SDK (giống trong Dockerfile) -----
      - name: Install Android SDK (sdkmanager)
        run: |
          rm -rf $HOME/.android $HOME/android-sdk
          mkdir -p $HOME/android-sdk/cmdline-tools
          cd $HOME/android-sdk
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q commandlinetools-linux-11076708_latest.zip
          mv cmdline-tools cmdline-tools-temp
          mkdir -p cmdline-tools/latest
          mv cmdline-tools-temp/* cmdline-tools/latest/
          rm commandlinetools-linux-11076708_latest.zip
          export ANDROID_SDK_ROOT=$HOME/android-sdk
          export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH
          # Chấp nhận license
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          # Cài platform-tools, platform 34, build-tools 34.0.0
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
          # Dọn dẹp
          rm -rf $ANDROID_SDK_ROOT/.temp $ANDROID_SDK_ROOT/.cache $HOME/.android/cache
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

      # ----- 5. CHUYỂN SANG JDK 11 ĐỂ BUILD (phù hợp với sourceCompatibility 1.8) -----
      - name: Setup JDK 11 for Gradle
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      # ----- 6. CÀI GRADLE 6.9.4 TOÀN CỤC (giống Dockerfile) -----
      - name: Setup Gradle 6.9.4
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '6.9.4'

      # ----- 7. FIX DEPENDENCIES (ép buộc Guava 31.1-android) -----
      - name: Fix dependencies
        run: |
          mkdir -p ~/.gradle/init.d
          cat > ~/.gradle/init.d/fix-all.gradle <<'EOF'
          allprojects {
            repositories {
              google()
              mavenCentral()
            }
            configurations.all {
              resolutionStrategy {
                force 'com.google.guava:guava:31.1-android'
                force 'com.google.guava:listenablefuture:9999.0-empty-to-avoid-conflict-with-guava'
                force 'com.google.guava:failureaccess:1.0.1'
                force 'androidx.lifecycle:lifecycle-viewmodel:2.6.2'
                force 'androidx.lifecycle:lifecycle-livedata:2.6.2'
                force 'androidx.lifecycle:lifecycle-runtime:2.6.2'
                force 'androidx.lifecycle:lifecycle-extensions:2.2.0'
                force 'androidx.arch.core:core-runtime:2.2.0'
                force 'androidx.core:core:1.12.0'
                force 'androidx.activity:activity:1.8.2'
                force 'androidx.appcompat:appcompat:1.6.1'
                force 'androidx.fragment:fragment:1.6.2'
                eachDependency { details ->
                  if (details.requested.group == 'com.google.guava') {
                    if (details.requested.name == 'guava') details.useVersion '31.1-android'
                    if (details.requested.name == 'listenablefuture') details.useVersion '9999.0-empty-to-avoid-conflict-with-guava'
                    if (details.requested.name == 'failureaccess') details.useVersion '1.0.1'
                  }
                  if (details.requested.group == 'androidx.lifecycle') details.useVersion '2.6.2'
                  if (details.requested.group == 'androidx.arch.core') details.useVersion '2.2.0'
                  if (details.requested.group == 'androidx.core') details.useVersion '1.12.0'
                  if (details.requested.group == 'androidx.activity') details.useVersion '1.8.2'
                }
              }
            }
          }
          EOF

      # ----- 8. BUILD MAINNETDEBUG APK -----
      - name: Build MainnetDebug APK
        run: |
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          # Chạy clean trước để tránh lỗi cache
          gradle :wallet:clean --no-daemon
          gradle :wallet:assembleMainnetDebug --stacktrace --no-daemon

      # ----- 9. KIỂM TRA FILE APK VÀ KÍCH THƯỚC -----
      - name: List APK files with sizes
        run: |
          find wallet/build/outputs/apk -name "*.apk" -exec ls -lh {} \;

      # ----- 10. UPLOAD APK LÊN ARTIFACT -----
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: bitcoin-wallet-mainnet-debug
          path: wallet/build/outputs/apk/mainnet/debug/*.apk